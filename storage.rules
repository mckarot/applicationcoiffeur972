rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Seuls les utilisateurs authentifiés peuvent écrire dans leur propre dossier "users/{userId}/"
    match /users/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}